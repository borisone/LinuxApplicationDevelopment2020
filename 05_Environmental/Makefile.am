bin_PROGRAMS = window
window_SOURCES = Window.c re.c

gitclean:
	git clean -fd

allclean:
	rm -rf .deps/ window *.m4 *.cache compile *.in *.status depcomp install-sh *.o *. config.* configure Makefile missing stamp-h1

